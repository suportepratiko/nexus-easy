# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env (nunca commitar secrets)
.env
.env.local
.env.*.local
.vapid_keys.json
.vapid_private.pem
.vapid_webpush.json

# Backend
backend/venv
bkp

# Agent / tooling artifacts (não é parte do app)
.agent

# Vite artifacts
.vite
vite.config.ts.timestamp-*.mjs

# Ops/instalações locais
app.zip
bun.lockb

# VAPID / webpush (estes arquivos são gerados/consumidos via `.vapid_webpush.json`)
.vapid_public_key
.vapid_cleared_subs

# Artefatos locais
=1.*
